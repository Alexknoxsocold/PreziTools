import { useQuery } from '@tanstack/react-query';
import { Badge } from '@/components/ui/badge';import { Card,CardContent } from '@/components/ui/card';
type Scorer={player:string;bestOdds:number;bestBook:string;modelProbability?:number;impliedProbability:number;edgePoints?:number;expectedValue?:number;qualifies?:boolean;reasons?:string[];dataStatus:'modeled'|'not-ready'};type MlModel={modelProbability:number;marketProbability:number;edgePoints:number;expectedValue:number;qualifies:boolean;reasons:string[]};type MlSide={team:string;bestOdds:number;bestBook:string;model:MlModel|null;dataStatus:'modeled'|'not-ready'};type Game={id:string;date:string;away:string;home:string;moneyline:{away:MlSide;home:MlSide}|null;anytimeGoal:Scorer[];firstGoal:Scorer[]};type Perf={totalRecorded:number;pending:number;graded:number;wins:number;losses:number;hitRate:number|null;units:number;roi:number|null;markets:{market:string;picks:number;wins:number;losses:number;hitRate:number|null;units:number;roi:number|null}[];modelPolicy:string;automaticRewriting:boolean};type Feed={modelVersion:string;moneylineModelVersion:string;marketStatus:string;games:Game[];health:{rawScorers:number;modeledScorers:number;coveragePct:number};performance:Perf};const odds=(n:number)=>n>0?`+${n}`:`${n}`;
function PlayerCard({p,rank}:{p:Scorer;rank:number}){const ready=p.dataStatus==='modeled';return <Card><CardContent className="p-4 space-y-3"><div className="flex justify-between gap-3"><div><div className="text-xs text-muted-foreground">#{rank}</div><div className="text-lg font-bold">{p.player}</div><div className="text-sm text-muted-foreground">{odds(p.bestOdds)} · {p.bestBook}</div></div><Badge>{p.qualifies?'OFFICIAL PLAY':ready?'MODEL LEAN':'DATA NOT READY'}</Badge></div><div className="grid grid-cols-4 gap-2 text-center text-sm"><div>Prezi<br/><b>{ready?`${p.modelProbability?.toFixed(1)}%`:'—'}</b></div><div>Book<br/><b>{p.impliedProbability.toFixed(1)}%</b></div><div>Edge<br/><b>{ready?p.edgePoints?.toFixed(1):'—'}</b></div><div>EV<br/><b>{ready?`${((p.expectedValue??0)*100).toFixed(1)}%`:'—'}</b></div></div>{p.reasons?.length?<div className="text-xs text-muted-foreground">{p.reasons.slice(0,3).join(' · ')}</div>:null}</CardContent></Card>}
function Market({title,rows}:{title:string;rows:Scorer[]}){return <section className="space-y-3"><h3 className="text-lg font-bold">{title}</h3>{rows.length?rows.map((p,i)=><PlayerCard key={p.player} p={p} rank={i+1}/>):<div className="rounded-lg border p-5 text-sm text-muted-foreground">NO PLAY — no qualifying market data available.</div>}</section>}
function Moneyline({m}:{m:Game['moneyline']}){if(!m)return <div className="rounded-lg border p-5 text-sm text-muted-foreground">DATA NOT READY — no moneyline market.</div>;return <section className="space-y-3"><h3 className="text-lg font-bold">Moneyline</h3><div className="grid gap-3 sm:grid-cols-2">{[m.away,m.home].map(s=>{const x=s.model;return <Card key={s.team}><CardContent className="p-4 space-y-2"><div className="flex justify-between gap-2"><div><b>{s.team}</b><div className="text-sm text-muted-foreground">{odds(s.bestOdds)} · {s.bestBook}</div></div><Badge>{x?.qualifies?'OFFICIAL PLAY':x?'MODEL LEAN':'DATA NOT READY'}</Badge></div>{x?<div className="grid grid-cols-4 text-center text-xs"><div>Prezi<br/><b>{x.modelProbability}%</b></div><div>Book<br/><b>{x.marketProbability}%</b></div><div>Edge<br/><b>{x.edgePoints}</b></div><div>EV<br/><b>{(x.expectedValue*100).toFixed(1)}%</b></div></div>:null}</CardContent></Card>})}</div></section>}
function Performance({p}:{p:Perf}){return <Card><CardContent className="p-5 space-y-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><h2 className="text-xl font-black">NHL V1 Forward-Test Performance</h2><p className="text-xs text-muted-foreground">Frozen production predictions · no automatic model rewriting</p></div><Badge variant="outline">{p.graded} graded · {p.pending} pending</Badge></div><div className="grid grid-cols-2 gap-3 md:grid-cols-5 text-center"><div><div className="text-xs text-muted-foreground">Record</div><b>{p.wins}-{p.losses}</b></div><div><div className="text-xs text-muted-foreground">Hit Rate</div><b>{p.hitRate==null?'—':`${p.hitRate}%`}</b></div><div><div className="text-xs text-muted-foreground">Units</div><b>{p.units>0?'+':''}{p.units}</b></div><div><div className="text-xs text-muted-foreground">ROI</div><b>{p.roi==null?'—':`${p.roi}%`}</b></div><div><div className="text-xs text-muted-foreground">Recorded</div><b>{p.totalRecorded}</b></div></div><div className="grid gap-2 md:grid-cols-3">{p.markets.map(m=><div key={m.market} className="rounded-lg border p-3 text-sm"><b>{m.market.replaceAll('-',' ').toUpperCase()}</b><div className="text-muted-foreground">{m.wins}-{m.losses} · {m.roi==null?'—':`${m.roi}% ROI`} · {m.units>0?'+':''}{m.units}u</div></div>)}</div></CardContent></Card>}
export default function NHL(){const {data,isLoading,error}=useQuery<Feed>({queryKey:['/api/nhl/markets'],queryFn:async()=>{const r=await fetch('/api/nhl/markets');if(!r.ok)throw new Error();return r.json()},refetchInterval:5*60*1000});if(isLoading)return <div className="py-12 text-center">Loading NHL models…</div>;if(error||!data)return <div className="py-12 text-center text-muted-foreground">NHL market data is temporarily unavailable.</div>;return <div className="space-y-6"><div><h1 className="text-3xl font-black">NHL Models</h1><p className="text-sm text-muted-foreground">Anytime Goal · First Goal · Moneyline · frozen V1 forward test</p></div><Performance p={data.performance}/><div className="flex gap-2"><Badge variant="outline">Coverage {data.health.coveragePct}%</Badge><Badge variant="outline">Modeled {data.health.modeledScorers}/{data.health.rawScorers}</Badge></div>{data.games.length?data.games.map(g=><section key={g.id} className="space-y-5 rounded-xl border p-4"><div><div className="text-xl font-black">{g.away} @ {g.home}</div><div className="text-xs text-muted-foreground">{g.date?new Date(g.date).toLocaleString():''}</div></div><Moneyline m={g.moneyline}/><div className="grid gap-6 lg:grid-cols-2"><Market title="Anytime Goal" rows={g.anytimeGoal}/><Market title="First Goal" rows={g.firstGoal}/></div></section>):<div className="rounded-xl border p-8 text-center text-muted-foreground">No NHL games with sportsbook markets are available yet.</div>}</div>}
