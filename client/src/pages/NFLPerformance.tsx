import { useQuery } from '@tanstack/react-query';
import { ArrowLeft, BarChart3, RefreshCw, ShieldCheck, Target, TrendingUp } from 'lucide-react';
import { Link } from 'wouter';
import { Button } from '@/components/ui/button';
import { Skeleton } from '@/components/ui/skeleton';

type Market={market:'anytime'|'first';sampleSize:number;hitRate:number|null;averageModelProbability:number|null;averageMarketProbability:number|null;brierScore:number|null;logLoss:number|null;marketBrierScore:number|null;flatUnitRoi:number|null};
type Performance={database:boolean;modelVersion:string;windowDays?:number;captured:number;graded:number;markets:Market[]};
const pct=(v:number|null|undefined)=>v==null?'—':`${(v*100).toFixed(1)}%`;
const score=(v:number|null|undefined)=>v==null?'—':v.toFixed(4);
const label=(m:string)=>m==='first'?'First TD':'Anytime TD';
function Metric({name,value,sub}:{name:string;value:string;sub?:string}){return <div className="rounded-xl border bg-card p-4"><div className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">{name}</div><div className="mt-2 font-mono text-2xl font-black">{value}</div>{sub&&<div className="mt-1 text-[10px] text-muted-foreground">{sub}</div>}</div>}
export default function NFLPerformance(){const q=useQuery<Performance>({queryKey:['/api/nfl/td-performance?days=90'],queryFn:async()=>{const r=await fetch('/api/nfl/td-performance?days=90');if(!r.ok)throw new Error('Unable to load NFL performance');return r.json()},refetchInterval:5*60*1000,staleTime:60*1000,retry:1});if(q.isLoading)return <div className="space-y-4"><Skeleton className="h-10 w-72"/><Skeleton className="h-28"/><Skeleton className="h-72"/></div>;const d=q.data;return <div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><Link href="/nfl" className="mb-2 inline-flex items-center gap-1 text-xs text-muted-foreground hover:text-foreground"><ArrowLeft className="h-3.5 w-3.5"/>NFL board</Link><h1 className="text-2xl font-black">NFL TD Model Performance</h1><p className="mt-1 max-w-3xl text-xs text-muted-foreground">Frozen pre-kickoff V2 predictions, graded against final results. Lower Brier and log loss are better; sportsbook Brier is shown on the same captured predictions.</p></div><Button variant="outline" size="sm" onClick={()=>q.refetch()} className="gap-2"><RefreshCw className={`h-3.5 w-3.5 ${q.isFetching?'animate-spin':''}`}/>Refresh</Button></div>{q.isError||!d?<div className="rounded-xl border border-destructive/30 bg-destructive/5 p-5 text-sm text-destructive">NFL touchdown performance could not be loaded.</div>:<><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><Metric name="Captured" value={String(d.captured)} sub="Pre-kickoff predictions"/><Metric name="Graded" value={String(d.graded)} sub="Final outcomes"/><Metric name="Model" value={d.modelVersion} sub={`${d.windowDays??90}-day window`}/><Metric name="Grade Rate" value={d.captured?pct(d.graded/d.captured):'—'} sub="Captured predictions resolved"/></div>{d.graded<50&&<div className="flex gap-3 rounded-xl border border-amber-500/30 bg-amber-500/10 p-4"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-amber-500"/><div><div className="text-sm font-bold">Early sample — collect before judging the model</div><div className="mt-1 text-xs text-muted-foreground">The dashboard is live, but TD props are noisy. Treat Brier, log loss, ROI and hit rate as descriptive until the graded sample is substantially larger.</div></div></div>}<div className="grid gap-5 lg:grid-cols-2">{d.markets.map(m=><section key={m.market} className="overflow-hidden rounded-xl border bg-card"><div className="border-b p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><Target className="h-4 w-4 text-primary"/><h2 className="font-black">{label(m.market)}</h2></div><span className="rounded-full border px-2 py-1 text-[10px] font-bold">n={m.sampleSize}</span></div></div><div className="grid grid-cols-2 gap-px bg-border sm:grid-cols-3"><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Hit Rate</div><div className="mt-1 font-mono text-xl font-black">{pct(m.hitRate)}</div></div><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Model Avg</div><div className="mt-1 font-mono text-xl font-black">{pct(m.averageModelProbability)}</div></div><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Market Avg</div><div className="mt-1 font-mono text-xl font-black">{pct(m.averageMarketProbability)}</div></div><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Prezi Brier</div><div className="mt-1 font-mono text-xl font-black">{score(m.brierScore)}</div></div><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Market Brier</div><div className="mt-1 font-mono text-xl font-black">{score(m.marketBrierScore)}</div></div><div className="bg-card p-4"><div className="text-[9px] uppercase text-muted-foreground">Log Loss</div><div className="mt-1 font-mono text-xl font-black">{score(m.logLoss)}</div></div></div><div className="flex items-center justify-between border-t p-4"><div className="flex items-center gap-2 text-xs font-semibold"><TrendingUp className="h-4 w-4 text-primary"/>Flat 1-unit ROI</div><div className={`font-mono text-lg font-black ${(m.flatUnitRoi??0)>=0?'text-emerald-500':'text-red-500'}`}>{pct(m.flatUnitRoi)}</div></div></section>)}</div>{!d.markets.length&&<div className="rounded-xl border bg-card p-10 text-center"><BarChart3 className="mx-auto h-8 w-8 text-muted-foreground/40"/><div className="mt-3 font-bold">Waiting for graded TD predictions</div><div className="mt-1 text-xs text-muted-foreground">Qualified predictions are being captured now. Metrics appear here after their games finish and are graded.</div></div>}<div className="rounded-xl border bg-muted/30 p-4 text-xs text-muted-foreground"><strong className="text-foreground">How to read this:</strong> hit rate tells you how often picks scored; Brier and log loss judge probability quality; market Brier provides the sportsbook benchmark on the same prediction set; flat-unit ROI grades the recorded best odds without changing stake size.</div></>}</div>}
